import styles from './scrollPhase.module.css'

export default function ScrollPhase() {

    return(
        <div className={`${styles.scrollPhase} flexRow cc flexGrow`}>
            <div className={styles.phaseImageContainer}>

            </div>
        </div>
    )
}